const user = ['Rupa', 3, 'Admin'];
var [name, courseCount, role] = user;
console.log(role);

const myUser = {
	name: 'Rupa',
	count: 3,
	role: 'Admin',
};

const { name, courseCount, role } = myUser;
console.log(courseCount);
